package day08;

public class SimpleSort {
    /**
     for (int i = 0; i < ints.length; i++) {
     int min = i;
     for (int j = i + 1; j < ints.length; j++) {
     if (ints[j] < ints[i]) {
     min = ints[j];
     }
     }  //  选出之后待排序中值最小的位置
     if (min != i) {
     int temp = ints[i];
     ints[i] = ints[min];
     ints[min] = temp;
     }  // 只要小于之前被选出位置对应的值，就交换
     }    // 简单选择排序

     */
}
